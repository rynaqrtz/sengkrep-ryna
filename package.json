{
  "name": "sengkrep-ryna",
  "version": "2.0.2",
  "description": "Scraping reliability layer for Node.js — schema validation, selector health monitoring, adaptive retry, fingerprint randomization, diff detection, response caching, cookie sessions, proxy rotation, rate limiting, interceptors, and JSON API extraction.",
  "main": "index.js",
  "keywords": [
    "web-scraping",
    "scraper",
    "reliability",
    "schema-validation",
    "diff-detection",
    "health-monitor",
    "fingerprint",
    "retry",
    "cache",
    "cookie-jar",
    "proxy-rotation",
    "rate-limiter",
    "interceptors",
    "json-extraction",
    "sitemap"
  ],
  "scripts": {
    "test": "node test/run.js && node test/integration.js",
    "example": "node example.js"
  },
  "author": "qrtz",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
